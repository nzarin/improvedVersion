Master thesis on peer discovery in federated overlays.

---------------------------------
Currently available: Node lookup in Kademlia (using Peersim Simulator)
---------------------------------

Builds upon the work from:

- Daniele Furlan (daniele.furlan@gmail.com)
- Maurizio Bonani (maurizio.bonani@gmail.com)


An example example.cfg configuration file can be found in the  main directory.

---------------------------------
Makefile
---------------------------------

To compile the sources, invoke:

  make

To compile the API documentation, invoke:

  make doc

To run the code, invoke:

  make run

To run all the previous command in this order, invoke:

  make all

